@model List<Application.Common.Dtos.RecipieComponentDto>

@{
    ViewData.TemplateInfo.HtmlFieldPrefix = "Components";
}

@for (int i = 0; i < Model.Count; i++)
{
    <li>
        <input class="form-control-plaintext" asp-for="@Model[i].SubTitle" onfocus="this.blur()"/>
        @{
            ViewBag.ComponentId = i;
        }
        <partial name="_IngredientsListPartial" model="Model[i].Ingredients" />
    </li>
}